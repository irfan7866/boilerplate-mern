{
  "name": "boilerplate-mern-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "build": "tsc -b",
    "copy": "cp -r src/* dist/",
    "dev": "if exist dist (rmdir /s /q dist && tsc -b && nodemon dist/bin/server.js) else (tsc -b && nodemon dist/bin/server.js)",
    "lint": "."
  },
  "keywords": [],
  "author": "Irfan Gouri",
  "license": "UNLICENSED",
  "dependencies": {
    "@types/express": "^4.17.21",
    "body-parser": "^2.0.0-beta.2",
    "express": "^5.0.0-beta.2",
    "mongoose": "^8.2.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
